version: "3.8"
services:
  db:
    image: "postgres:12-alpine"
    restart: always
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_USER: username
    ports:
      - 5432:5432

  adminer:
    image: adminer
    restart: always
    ports:
      - 9000:8080
